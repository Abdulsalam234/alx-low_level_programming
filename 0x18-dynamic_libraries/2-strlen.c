#include "main.h"
#include <string.h>
/**
 * _strlen - prints out the lenght of a string
 * @s: pointer value of char
 * @value: assigned value to pointer
 * Return: returns length of string
 */
int _strlen(char *s)
{
	int value;

	value = strlen(s);
	return (value);
}
