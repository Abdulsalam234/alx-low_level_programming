#include "main.h"


void *_memset(void *s, int b, size_t n)
{
	unsigned int i;
	unsigned char *mem;
	unsigned char value;
	s = *mem;
	c = value;

	for (i = 0; i < n; i++)
	{
		mem[i] = value;
	}
	return (mem);
}
